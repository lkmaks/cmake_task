#include "main.h"
#include <iostream>

int main() {
    std::cout << sqr(2) << std::endl; // in index.h
    std::cout << cub(2) << std::endl; // in lib.h
    std::cout << fou(2) << std::endl; // in main.h
}
